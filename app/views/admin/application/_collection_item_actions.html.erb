<% if display_resource_name(page.resource_name) == "Evènements" %>
  <% if existing_action?(collection_presenter.resource_name, :edit) %>  
  <td><%= link_to(
    t("administrate.actions.edit"),
    [resource],
    class: "action-edit",
  ) if accessible_action?(resource, :edit) %></td>
  <% end %>  


<% elsif display_resource_name(page.resource_name) == "Utilisateurs"  %>
  <% if existing_action?(collection_presenter.resource_name, :edit) %>
    <td><%= link_to(
      t("administrate.actions.edit"),
      [:edit, namespace, resource],
      class: "action-edit",
    ) if accessible_action?(resource, :edit) %></td>
  <% end %>     
    
<% else %>

<% end %>  

